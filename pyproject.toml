[project]
name = "claudegpt"
version = "0.1.0"
description = "Anthropic Messages API to Azure OpenAI proxy for Claude Code"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
